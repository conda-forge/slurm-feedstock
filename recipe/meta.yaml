{% set version = "22.05.5" %}

package:
  name: slurm
  version: {{ version }}

source:
  url: https://download.schedmd.com/slurm/slurm-{{ version }}.tar.bz2
  sha256: f687c98c4f7c0b7409f865771bbb05986daa3e207616667a9aa7390ba5a50fce

build:
  skip: true  # [not linux]
  number: 0

requirements:
  build:
    - make
    - {{ compiler("c") }}
    - {{ compiler("cxx") }}
  host:
    - munge
    - mysql-devel
    - bzip2
    - libzlib
    - zstd
    - openssl
    - libcurl
    - krb5
    - keyutils
    - libedit
  run:
    - munge

test:
  commands:
    - slurmd -h
    - slurmctld -h
    - test -f $PREFIX/bin/sacct
    - test -f $PREFIX/bin/salloc
    - test -f $PREFIX/bin/sprio
    - test -f $PREFIX/bin/srun
    - test -f $PREFIX/bin/sshare
    - test -f $PREFIX/bin/sstat
    - test -f $PREFIX/bin/strigger

about:
  home: https://slurm.schedmd.com/
  summary: cluster management and job scheduling system for Linux clusters
  license: GPL-2.0-only
  license_family: GPL2
  license_file: COPYING

extra:
  recipe-maintainers:
    - msarahan
